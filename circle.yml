machine:
  environment:
    NODE_ENV: test
  node:
    version: 0.10.35
dependencies:
  override:
    - npm install --dev
general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore