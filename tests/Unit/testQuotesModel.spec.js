var Quote = require('../../api/models/Quotes'),
    sinon = require('sinon'),
    assert = require('assert');
/*
describe('quotes CRUD', function() {

    describe('creates a quote', function(done) {
        Quote.create({
            text: "test quote",
            author: 'test author'
        }, done);
    });

    describe('creates fetches a quote', function(done) {
        assert.fail(true, 'quote not fetched');
        done();
    });


    describe('creates updates a quote', function(done) {
        assert.fail(true, 'quote not updated');
        done();
    });

    describe('creates destroys a quote', function(done) {

        var quote;

        beforeEach(function(done) {
            quote = Quote.create({
                text: "test quote",
                author: 'test author'
            });
        });

        afterEach(function(done) {

        });

        it('creates destroys a quote', function(done) {
            quote.destroys();
            assert.ok(quote == null, 'quote not destroyed');
            done();
        });
    });

});
*/